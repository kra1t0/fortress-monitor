# fortress-monitor
FORTRESS MONITOR v2.1 — Zero-Dependency Edition   Comprehensive SSH Brute Force Detection &amp; Security Auditing   Target: Ubuntu 24.04.4 LTS    ZERO external packages. Uses only Python 3 standard library   + free public API (ip-api.com) for geolocation.
